# 传统后台管理壳层布局改造计划

## 1. 状态、结论与停止点

- 文档状态：**阶段一、阶段二 A、阶段二 B、阶段二 C、阶段三 A、阶段三 B1a、阶段三 B1b、阶段三 B2、阶段三 C
  均已完成并进入当前基线（2026-08-09）**。
- 阶段一可行性结论：**可行**。当前 React 应用已经由统一 `AppFrame` 承载普通顶栏、路由内容、失败通知和调度队列，
  可以在不修改 Backend、数据库和业务接口的前提下改为“左侧菜单 + 右侧全局顶栏 + 内容区”的传统后台布局。
- 新增需求可行性结论：**有边界地可行**。设置模块二级菜单和“点击行展示详情”属于前端信息架构调整；连接新增/删除
  需要新增 Standard Provider 创建、删除接口和引用保护，但现有 `code_quality_model_providers.built_in` 已能区分内置与
  自定义记录，不需要新表或数据库迁移。Agent Runtime 仍是固定 Worker 契约，不纳入动态增删。
- 风险等级：**中高**。阶段一、二的布局风险已完成收敛；新增阶段的主要风险转为 Provider 误删、配置引用悬空、设置
  子路由兼容、dirty 草稿跨菜单丢失，以及桌面折叠、平板浮层和移动 Drawer 中的二级菜单一致性。
- 当前停止点：**阶段三 C 已完成全量回归和三档合并浏览器验收，54 专项全部计划阶段完成**。当前停止等待用户下一步
  指令，不得自动提交、推送或部署。

## 2. 需求目标与参考边界

### 2.1 用户目标

将当前横向顶部导航调整为传统后台管理布局：

- 左侧展示品牌和主菜单；
- 右侧工作区顶部展示跨页面的全局信息和操作；
- 页面主体继续承载现有运行总览、任务、设置和其它业务页面；
- 桌面侧栏可折叠，并兼容平板、移动端和现有 Review 沉浸工作台。

壳层改造完成后，在同一设置页面内将 Agent Review 与 Standard Review 的运行配置、模型连接目录和原生详情编辑器
聚合为一个工作台，使用户能够在一个上下文中理解两种 Review 的当前选择、连接状态和配置入口。

阶段三继续完成以下调整：

- 将当前四个设置模块作为“设置”一级菜单下的二级菜单，以独立子路由一次只展示一个模块；
- 模型连接目录整行默认可点击、可聚焦并直接展示详情，移除冗余“详情”按钮；
- 支持创建和删除自定义 Standard Provider，同时保护 Agent Runtime、内置 Provider、默认 Provider 和仍被配置引用的
  Provider。

### 2.2 参考图使用边界

参考图只用于壳层位置、信息层级、留白和操作归属，不作为页面能力清单。阶段一、二不照搬参考图中的知识库、成员与
权限、审查规则、用户头像或管理员菜单，也不拆分当前设置页；阶段三只把已经存在的四个设置模块映射为二级菜单，不
新增参考图中的其它设置页面。

本专项遵循“布局迁移，不重做信息架构”：

- 保留现有路由、页面文案和功能开关；
- 不因侧栏可容纳更多项目而开放隐藏中的质量治理入口；
- 不伪造当前系统没有的登录用户身份；
- 阶段一不改变各业务页面内部卡片、表格、表单和工作流；阶段二只聚合设置页的模型配置入口，不改变配置语义。

模型配置参考图同样只作为信息层次和列表/详情布局参考。阶段二没有照搬以下当时不存在的能力：

- 不增加或删除连接；Provider 继续使用 Backend 返回的固定目录；
- 不持久化普通 Provider 的最近测试时间和延迟，测试结果只在当前详情会话展示；
- 不伪造独立的 Standard Review 开关，平台级能力仍由现有全局开关控制；
- 不提供静态 Agent fallback Provider 下拉框；Agent 失败后继续执行项目端类型、项目、项目组模型、Profile 和全局默认
  Provider 组成的 Standard 动态解析链；
- 不把 Agent 与 Standard 称为共享连接池，不复用 Agent 独立加密 Key 和普通 Provider Key。

阶段三对上述历史边界作两个显式扩展：新增设置子路由，以及新增/删除**自定义 Standard Provider**。以下边界继续
有效：不建设真正共享连接池；不动态注册或删除 Agent Runtime；不删除 Backend 初始化维护的内置 Provider；不持久化
最近测试或延迟；不增加用户、权限、知识库、审查规则或虚构身份能力。

## 3. 当前实现与改造基础

当前普通应用壳层集中在 `frontend/src/App.jsx` 的 `AppFrame`：

- `Header.app-header` 同时承载品牌、运行总览、任务、质量治理、设置、版本更新、接入帮助、失败通知和调度队列；
- `Content` 直接承载全部 React Router 路由；
- 失败通知与调度队列的状态、轮询、打开动作和 Modal 已经由 `AppFrame` 统一管理；
- `ReviewWorkspaceModeContext` 会在当前任务进入沉浸模式时隐藏普通 Header，并为 Content 切换沉浸样式；
- `frontend/src/styles.css` 已经以 `760px`、`1000px`、`1180px` 等断点处理普通顶栏和沉浸工作台；
- Command Center 使用 `calc(100dvh - 56px)` 计算普通壳层下的最小高度。

因此阶段一无需改变页面级数据流，核心工作是重组 `AppFrame` 的展示结构，并统一壳层尺寸和断点契约；阶段二继续复用
现有设置请求，只在前端建立统一展示模型和分域操作入口。

## 4. 目标壳层结构

普通页面采用嵌套 Layout，页面继续使用文档级滚动，不新增独立内容滚动容器：

```text
AppFrame
├─ AppSidebar：品牌、主导航、折叠控制
└─ MainLayout
   ├─ GlobalHeader：移动端菜单、版本、帮助、失败通知、调度队列
   └─ Content：现有 Routes、JobQueueModal、FailureNotificationsModal
```

目标 DOM 和样式必须满足：

- 外层最小高度为 `100dvh`，右侧 MainLayout 设置 `min-width: 0`，避免表格或长内容撑破文档宽度；
- 侧栏占满视口高度并保持在左侧，品牌区与右侧顶栏统一为 `56px` 高；
- 右侧顶栏使用 `position: sticky; top: 0`，页面继续由浏览器文档滚动，避免 Drawer、Modal 和沉浸页面出现双滚动；
- Content 不统一增加新的最大宽度或卡片容器，保留各页面现有的 `TaskWorkspaceShell`、`page-shell` 和
  Command Center 布局规则；
- 普通壳层隐藏时，侧栏、顶栏及其宽度占位必须一起移除，不能只设置视觉隐藏。

## 5. 导航与全局操作契约

### 5.1 左侧导航

| 导航项 | 路由或条件 | 选中规则 |
| --- | --- | --- |
| 运行总览 | `/` | 仅根路由精确匹配 |
| 任务 | `/tasks` | `/tasks` 及 `/tasks/:taskId` |
| 质量治理 | 继续受 `QUALITY_GOVERNANCE_NAV_VISIBLE` 控制 | 按现有质量治理子路由最长前缀匹配 |
| 设置 | `/settings` 与 `/settings/*` | `/settings` 前缀匹配；阶段三同时精确高亮当前二级菜单 |

质量治理启用时，继续复用现有质量看板、评估样本、规则缺口、验收记录、回放记录和受独立开关控制的反馈池；横向
Dropdown 改为侧栏子菜单，但路由、顺序、图标和功能开关语义不变。当前开关关闭时不得显示空分组或占位项。

侧栏品牌区使用现有 Ant Design 图标体系和“AI代码质量审查平台”文本，不增加图片、远程资源或新依赖。折叠时只展示
可识别的品牌图标，品牌按钮仍可返回根路由，并保留明确的 `aria-label`。

阶段三在“设置”下增加的二级菜单只映射第 9.3.1 节四个现有模块；不与质量治理分组混用，不受质量治理功能开关影响，
也不改变运行总览、任务和沉浸工作台的一级导航顺序。

### 5.2 右上全局区域

右上操作固定按以下顺序展示：

1. 帮助（复用接入帮助路由）；
2. 版本（复用版本更新路由）；
3. AI Review 失败通知；
4. AI Review 调度队列。

“帮助”和“版本”分别复用现有接入帮助、版本更新路由；失败通知继续复用失败数量、危险态和原 Modal；调度队列继续复用活动数量 Badge、
轮询、取消和打开任务行为。不得修改请求地址、刷新周期、错误降级或 `AppFrameOperationsContext` 对外能力。

右上区域不显示“管理员”、头像、账户菜单或退出入口。未来接入真实身份体系时应另建专项，不在本布局中预留虚假数据。

### 5.3 路由与交互

- 使用 Ant Design Menu 的选中和展开能力承载主导航，不用仅靠颜色区分当前页面；
- 导航后继续传递现有 `location.state.from` 语义，详情页返回行为不变；
- 折叠图标需提供 Tooltip，所有图标按钮需提供可访问名称和清晰的键盘焦点；
- 移动端 Drawer 选择导航后自动关闭，Escape、遮罩点击和显式关闭按钮均可关闭，并将焦点还给菜单触发按钮；
- 浏览器前进、后退和直接访问详情路由时，菜单选中状态必须由 `location.pathname` 派生，不能依赖点击时保存的状态。

## 6. 响应式与折叠状态契约

### 6.1 桌面：视口宽度 `>= 1200px`

- 侧栏展开宽度 `224px`，折叠宽度 `72px`，首次访问默认展开；
- 折叠只影响品牌文本和菜单标签，不隐藏菜单项或改变路由；
- 折叠按钮位于侧栏底部，具有“收起菜单”或“展开菜单”的动态可访问名称；
- 用户主动折叠或展开后，将布尔偏好保存到
  `ai-code-review.app-shell.sidebar-collapsed.v1`。

存储值只接受字符串 `true` 或 `false`；缺失、读取异常或其它值一律按 `false`，即默认展开。写入失败不得阻断导航或
页面渲染。

### 6.2 平板：视口宽度 `761px–1199px`

- 固定保留 `72px` 图标栏，避免展开侧栏持续压缩表格和 Command Center；
- 点击展开按钮时，以覆盖内容的临时浮层展示 `224px` 完整菜单，不改变右侧内容宽度；
- 浮层选择菜单、点击外部或按 Escape 后关闭，并恢复触发按钮焦点；
- 平板自动折叠和临时展开不读写桌面折叠偏好，回到桌面时恢复用户原有桌面选择。

### 6.3 移动端：视口宽度 `<= 760px`

- 不渲染固定侧栏或其宽度占位；
- 顶栏左侧展示菜单按钮和精简品牌，菜单按钮打开移动端 Drawer；
- Drawer 宽度为 `min(280px, 88vw)`，包含完整品牌、主导航和折叠侧栏中的全部可见入口；
- 右上全局操作保留四项真实能力，空间不足时“帮助”和“版本”使用图标与 Tooltip/`aria-label`，不删除功能；
- 移动端 Drawer 开关为会话内临时状态，不写入桌面折叠偏好。

### 6.4 断点切换

- 从移动端或平板切换到桌面时，必须关闭 Drawer/浮层并恢复桌面偏好；
- 从桌面切换到平板或移动端时，不覆盖已经保存的桌面偏好；
- 任一断点都不得出现侧栏和 Drawer 同时可交互、不可见遮罩拦截页面、横向页面滚动或壳层双滚动。

## 7. Review 沉浸模式兼容

`ReviewWorkspaceModeContext` 和 `resolveReviewWorkspaceFrame` 继续作为普通壳层是否显示的唯一业务来源：

- 当前任务详情为沉浸模式时，不渲染 AppSidebar 和 GlobalHeader，Content 不保留左侧宽度、顶栏高度或普通页面 padding；
- terminal、非法模式、离开任务详情路由和组件卸载时，继续恢复普通壳层；
- 沉浸模式下不显示或后台保留移动 Drawer、平板浮层及其遮罩；进入沉浸模式前应关闭这些临时层；
- 不修改 ReviewJourney、当前 `reviewKey`、轮询、Canvas、Drawer、Popover、Finding、Diff、Patch 或结果数据；
- 普通壳层的折叠偏好不因进入或退出沉浸模式而改变。

## 8. 范围与非目标

### 8.1 本专项负责

- `AppFrame` 普通壳层结构和展示状态；
- 左侧导航模型、路由选中和现有质量治理分组迁移；
- 桌面折叠、平板临时浮层、移动端 Drawer 和偏好存储；
- 右上全局操作迁移；
- 普通页面、Command Center 和 Review 沉浸模式的布局兼容；
- Agent Review 与 Standard Review 运行配置的统一视图；
- Agent Runtime 和 Standard Provider 的统一连接目录、原生详情编辑器与响应式布局；
- Provider 详情保存和 Standard 默认 Provider 保存的职责拆分；
- 设置一级菜单下四个现有模块的二级菜单、子路由和 dirty 离开保护；
- 模型连接目录的整行选择、详情按钮移除和自定义 Standard Provider 生命周期管理；
- Standard Provider 创建、删除 API、内置保护和配置引用保护；
- 相关纯函数、组件、样式、前端自动化测试和浏览器验收。

### 8.2 非目标

- 不修改任何页面的业务内容、表单、表格、筛选或卡片设计；
- 除阶段三明确的四个现有设置子路由外，不增加成员、权限、知识库或审查规则页面；
- 不新增用户身份、登录、头像、账户菜单或权限接口；
- 不开放当前隐藏的质量治理能力；除设置子路由外，不增加或重命名其它路由；
- 阶段一、二不修改 Backend、数据库或 API；阶段三只新增 Standard Provider 创建、删除契约及其请求 DTO，不修改通知、
  队列、Review 执行或 Agent API；
- 不增加新的前端依赖、图片资源、远程字体或主题系统；
- 不新增、删除或动态注册 Agent Runtime，不删除内置 Standard Provider；
- 不统一两类 Key 的存储，不新增连接表、测试结果表或数据库迁移；阶段三新增的 Provider POST / DELETE 继续使用现有
  `code_quality_model_providers` 表；
- 不持久化普通 Provider 的最近测试、延迟或测试响应；
- 不改变 Agent Worker、出站、预算、任务快照、Standard fallback 或 Provider 解析优先级；
- 不修改 `README.md`、`docs/36` 或已关闭专项的状态。

## 9. 前端实现设计

### 9.1 阶段一：传统后台壳层

实施时将导航配置和路径选中规则从 `AppFrame` 的 JSX 中提取为可独立测试的纯展示模型，`AppFrame` 继续负责现有全局
请求、轮询和 Modal 状态。壳层组件只接收以下展示和操作输入：

- 当前 pathname 和功能开关；
- 桌面折叠、平板浮层、移动 Drawer 状态；
- 导航回调；
- 失败通知数量、调度活动数量和两个打开动作；
- 是否处于 Review 沉浸模式。

不新增公开 HTTP 接口或业务 schema。唯一新增的持久化界面是浏览器本地偏好键
`ai-code-review.app-shell.sidebar-collapsed.v1`；该键不承载身份、任务、Review 或其它业务数据。

样式应使用明确的壳层 CSS 变量统一 `56px` 顶栏高度、`224px` 展开宽度和 `72px` 折叠宽度，并审计现有
`100vh/100dvh - 56px/64px` 计算。只修正由壳层尺寸变化直接造成的高度和宽度问题，不顺带重构页面样式。

### 9.2 阶段二：统一 Review 模型配置工作台

#### 9.2.1 聚合边界

将现有“模型 Provider 配置”和“Agent Review”两个折叠项合并为一个“模型连接与 Review 配置”折叠项；项目组、
Profile、全局设置及其它折叠项的顺序和行为不变。

统一只发生在展示和操作入口，Backend 仍保持三套原生契约：

| 配置域 | 读取 | 保存 / 操作 | 凭据与测试 |
| --- | --- | --- | --- |
| 平台 Standard 默认值 | `GET /api/code-quality-reviews/settings` | `POST /api/code-quality-review-providers/{code}/set-default` | 不包含 Key，不发起模型请求 |
| Agent Review | `GET /api/code-quality-reviews/agent-settings` | `PUT /api/code-quality-reviews/agent-settings` | 独立加密双 Key；`POST .../agent-settings/test` 异步测试 |
| Standard Provider | `GET /api/code-quality-review-providers` | `PUT /api/code-quality-review-providers/{code}` | Provider 自有 Key；`POST .../{code}/test` 同步测试 |

不新增聚合 API。页面继续并行读取三份数据，前端通过纯展示模型生成运行配置卡和连接目录；任一请求失败时沿用设置页
整体错误处理，不以另一份成功数据伪造完整配置。

#### 9.2.2 顶部运行配置

顶部使用两张等高运行配置卡：

- **Agent Review**：真实启用开关、运行时选择、当前模型、Custom Runtime 推理强度、Worker Pool、Key 和配置完整性状态；
  fallback 只读显示“失败后继承 Standard 动态解析链”，不可直接选择某个 Provider；
- **Standard Review**：默认 Provider 选择、只读协议、模型、超时、Provider 启用和 Key 状态；不增加独立 Standard
  开关，平台全局启用状态只作为只读状态展示并继续在“全局设置”中修改。

两张卡均使用显式保存：Agent 保存调用现有 Agent Settings PUT；Standard 的“保存默认连接”只调用现有 set-default。
选择未配置完整的 Agent Custom Runtime 时，页面切换到对应详情并提示先补齐配置，不提交半成品；选择未启用或未配置 Key
的 Standard Provider 时允许保持现有 Backend 兼容行为，但必须展示明确警告，不能把它显示为可用。

#### 9.2.3 模型连接目录

目录按 Agent Runtime 在前、Standard Provider 在后生成稳定行：

- Agent 行 ID：`AGENT:<runtimeType>`；
- Standard 行 ID：`STANDARD:<providerCode>`；
- 列：名称、所属 Review、协议、Endpoint、模型、默认/当前状态、配置状态、更新时间和详情操作；
- Agent 的 `updatedAt` 属于整个 Agent Settings，只作为设置更新时间展示，不宣称是单个 Runtime 的独立更新时间；
- 不展示不可可靠恢复的最近测试时间和延迟，不提供新增、删除、分页或使用范围复选框。

初次加载默认选中 Standard 默认 Provider；不存在时选中 Agent 当前 Runtime，再回退到首行。重新加载数据时，原行仍存在
则保持选中，否则重新应用上述顺序。Standard 行只标识“Standard 默认”或“可供 Standard 使用”；不能根据 Provider
列表猜测项目、项目组、Profile 或 Agent fallback 的实际使用关系。

#### 9.2.4 原生详情编辑器

目录右侧或下方只渲染当前行对应的原生详情：

- Agent 默认 Runtime：固定 Endpoint/模型、独立 DeepSeek Key、保存、异步配置测试和清除 Key；
- Agent Custom Runtime：名称、只读 Responses 协议、Base URL、模型、推理强度、TLS 校验、独立 Key、安全状态、
  Worker 支持、保存、异步测试和清除 Key；
- Standard Provider：名称、只读协议类型、Endpoint、模型、超时、Provider Key、启用状态、保存、同步测试和清除 Key；
- Agent 执行预算保留为工作台底部的独立全宽区域，不归属于当前连接行，也不因选择 Standard Provider 而消失。

Provider 详情保存只调用 Provider PUT，不再隐式调用 set-default；设置默认 Provider 只由顶部 Standard 卡负责。Agent 测试
继续先保存当前 Agent 草稿并按 requestId 轮询，Provider 测试继续使用当前未保存草稿且只在会话内展示结果。

页面为当前详情维护 dirty 状态。用户选择其它目录行时，如名称、Endpoint、模型、超时、启用状态、TLS、推理强度或 Key
草稿存在未保存变化，先弹出丢弃确认；确认后恢复该连接最近一次服务端数据，取消则保持当前行。保存或清除 Key 成功后
清除 dirty；页面路由离开行为保持现状，不新增全局 beforeunload 拦截。

#### 9.2.5 响应式

- `>= 1200px`：顶部双卡；下方目录和详情双栏，目录优先获得宽度，详情最小宽度 `360px`；
- `761px–1199px`：顶部双卡保持并排，目录和详情上下排列，不让侧栏或详情压缩表格；
- `<= 760px`：全部单列，目录隐藏次要列，将协议、Endpoint 和模型收敛到名称单元格的辅助信息；阶段二详情按钮保持
  至少 `44px` 可点击高度，阶段三移除该按钮后由整行承担相同可点击高度；
- 三档均不使用固定内容高度，保持文档滚动，不新增横向页面滚动或嵌套滚动区。

### 9.3 阶段三：设置二级菜单与自定义 Provider 生命周期

#### 9.3.1 设置二级菜单和子路由

“设置”继续作为一级菜单，只把当前已经存在的四个模块映射为二级菜单，不复制模块、不新增参考图中的页面：

| 二级菜单 | 子路由 | 对应现有模块 |
| --- | --- | --- |
| 项目组 / 端类型配置 | `/settings/project-targets` | `project-target-configs` |
| AI Review 配置 | `/settings/review-profiles` | `profile-settings` |
| 模型连接与 Review 配置 | `/settings/model-connections` | `review-model-settings` |
| 全局设置 | `/settings/global` | `global-settings` |

历史 `/settings` 使用 `replace` 重定向到 `/settings/model-connections`，避免旧书签失效且直接进入本轮重点页面。设置页不再
以 Collapse 同时渲染四个模块；父级 Settings 容器继续复用现有状态和请求控制器，按子路由只展示当前模块。浏览器刷新、
前进、后退和直接访问子路由时，一级“设置”和对应二级菜单必须同时高亮。

- 桌面展开侧栏直接显示四个二级菜单；桌面折叠时通过可聚焦浮层展示；
- 平板 `72px` 图标栏点击“设置”后，在现有菜单浮层中展示二级菜单，不挤压内容；
- 移动端 Drawer 展示嵌套菜单，选择二级菜单后关闭 Drawer 并恢复焦点；
- 当前模块存在未保存草稿时，二级菜单切换、其它一级菜单、浏览器前进/后退和关闭页面均进入统一离开保护；取消后保留
  当前路由和焦点，确认后恢复最近服务端草稿再导航；
- 不为二级菜单增加独立本地持久化状态；展开侧栏时“设置”分组默认展开，折叠/平板/移动状态继续沿用阶段一壳层契约。

#### 9.3.2 目录选择和操作列

连接目录整行成为详情选择入口，继续支持鼠标点击、Enter / Space、`aria-selected`、选中高亮和 dirty 丢弃确认。移除
“详情”按钮及其操作列文案，不再提供同一选择动作的重复入口。

- 桌面操作列只为 `builtIn=false` 的自定义 Standard Provider 展示危险态删除按钮；点击删除必须阻止行选择事件冒泡，
  未知引用仍由 Backend 删除校验裁决；
- Agent Runtime、内置 Provider 不显示删除按钮；当前默认自定义 Provider 的按钮禁用，并以 Tooltip 说明保护原因；
- 移动端隐藏操作列，删除入口放在当前自定义 Provider 详情底部的危险操作区；
- 删除当前选中行成功后，按“Standard 默认 -> Agent 当前 Runtime -> 首行”重新选择；删除其它行保持当前选择；
- 新增 Provider 成功后刷新目录并选中新行，但不自动设为 Standard 默认、不自动启用 Agent，也不触发联通性测试。

#### 9.3.3 自定义 Standard Provider 创建契约

新增 `POST /api/code-quality-review-providers`。请求使用明确 DTO，字段如下：

| 字段 | 规则 |
| --- | --- |
| `providerCode` | 必填、转大写，匹配 `^[A-Z][A-Z0-9_]{0,63}$`，创建后不可修改且全局唯一 |
| `providerName` | 必填，去除首尾空白，长度 `1–128` |
| `providerType` | 必填，只允许 `OPENAI_CHAT_COMPATIBLE`、`OPENAI_RESPONSES`、`ANTHROPIC_MESSAGES` |
| `endpointUrl` | 可选；创建为启用时必须非空，测试和执行继续沿用现有 Provider Endpoint 行为 |
| `modelName` | 可选，最长 `128` |
| `timeoutSeconds` | 可选，范围 `1–3600`，继续复用现有超时校验 |
| `apiKey` | 可选写入，响应只返回 `apiKeyConfigured` / `apiKeyMasked`，绝不回传明文 |
| `enabled` | 可选，默认 `false`；配置不完整时不允许创建为启用 |

Backend 强制写入 `built_in=false`，`sort_order` 取当前最大值后的稳定步长；响应 `data` 返回新建 Provider，前端随后刷新现有
GET 目录。重复 Code 返回 `409 PROVIDER_ALREADY_EXISTS`，非法字段返回现有 `400 VALIDATION_ERROR`。创建不修改平台默认
Provider、项目/项目组/Profile 覆盖或 Agent fallback 解析链。

前端“新增模型连接”按钮位于连接目录标题区，打开 Modal 收集上述字段；协议选择后只展示真实支持字段。保存成功关闭
Modal、刷新并选中新行；失败保留非 Key 草稿和错误信息，Key 输入继续留在组件内存且不写日志、URL 或浏览器持久化。

#### 9.3.4 自定义 Standard Provider 删除契约

新增 `DELETE /api/code-quality-review-providers/{providerCode}`，只允许删除 `built_in=false` 的 Standard Provider。删除前在
同一事务中执行以下保护：

- 内置 Provider 返回 `409 PROVIDER_BUILT_IN`；
- 当前平台默认 Provider 返回 `409 PROVIDER_IS_DEFAULT`；
- `code_quality_review_profiles.provider_code`、`projects.default_code_quality_provider_code`、
  `project_groups.default_provider_code`、`project_group_ai_review_models.provider_code` 或
  `project_target_configs.provider_code` 仍有引用时，返回 `409 PROVIDER_IN_USE`，错误信息列出阻塞配置域及数量；
- 不存在返回 `404 RESOURCE_NOT_FOUND`；并发下再次校验上述条件，不能先查后删留下竞态窗口。

历史 Review 结果和进度中保存的是执行快照字符串，不作为删除阻塞项，也不得级联删除或改写。成功响应
`data={"providerCode":"...","deleted":true}`；删除会永久移除该自定义 Provider 及其凭据，不提供软删除恢复。

前端仅对可删除行开放入口。删除前先处理当前 dirty 草稿，再展示不可恢复警告，并要求用户输入完整 Provider Code 二次
确认；成功后刷新目录，失败时保留当前详情和选择。禁止通过前端隐藏按钮代替 Backend 引用保护。

## 10. 风险与回退

| 风险 | 控制方式 |
| --- | --- |
| 侧栏压缩后表格或 Command Center 溢出 | 桌面/平板采用不同展开策略，MainLayout 设置 `min-width: 0`，执行三档浏览器验收 |
| 沉浸模式残留侧栏宽度或顶栏高度 | 由 `reviewWorkspaceFrame.immersive` 控制整个普通壳层是否渲染，补路由切换回归 |
| 响应式切换留下 Drawer 遮罩 | 断点变化和沉浸模式变化时统一关闭临时层，覆盖焦点恢复测试 |
| localStorage 异常阻断应用 | 读写封装为容错纯逻辑，非法值回退默认展开，异常不向业务层传播 |
| 通知或队列迁移后停止刷新 | 保留现有状态、生命周期和 Context，只调整触发器位置，补请求与打开行为源码契约 |
| 全局顶栏拥挤 | 宽屏显示文字，窄屏使用图标和可访问名称，不删除真实操作 |
| 统一目录误导为共享 Key | 使用 Agent / Standard 域标签和原生详情，文案明确“统一视图、分域保存” |
| Provider 保存意外切换默认值 | Provider PUT 与 set-default 动作分离，分别补请求契约测试 |
| 目录切换丢失未保存配置或 Key | dirty 状态和丢弃确认；确认后从最近一次服务端数据恢复 |
| Agent fallback 被误解为固定 Provider | 顶部只读展示动态解析链，不提供静态 Provider 下拉框 |
| 参考图状态列无法由现有 API 支撑 | 只展示真实配置状态；不伪造最近测试、延迟和实际项目使用范围 |
| 设置子路由切换丢失模块草稿 | 建立统一 dirty 注册和 Router blocker，覆盖菜单、前进后退、刷新/关闭及焦点恢复 |
| 删除内置、默认或在用 Provider | Backend 事务内强制校验 `built_in`、平台默认和五类配置引用；前端只作辅助提示 |
| 新增 Provider 绕过协议或完整性边界 | 创建 DTO 限定三种已支持协议，创建为启用时强制 Endpoint、模型和 Key 完整 |
| 行删除按钮误触发详情选择 | 删除动作阻止事件冒泡并要求输入 Provider Code 二次确认，移动端放入详情危险区 |
| 自定义 Provider 删除后历史结果不可读 | 历史结果保留执行快照字符串且不级联；展示层对已删除 Code 使用安全文本回退 |

本专项没有数据迁移。阶段一回退时恢复原普通 Header 和相关样式，新增 localStorage 键即使保留也不会影响旧版业务；
阶段二回退时恢复原“模型 Provider 配置”和“Agent Review”两个折叠项及 Provider 保存后设默认的旧交互，不需要恢复
Backend 或数据库。

阶段三 A 可回退为 `/settings` 单页 Collapse 和“详情”按钮，不涉及 Backend；阶段三 B1a/B1b 尚未开放前端入口，若
回退 POST / DELETE，已创建的自定义 Provider 仍是现有表中的合法 `built_in=false` 记录，可继续通过 GET / PUT 使用；阶段
三 B2 回退时隐藏新增/删除入口并停止调用上述接口。已成功硬删除的自定义 Provider 及凭据不可自动恢复，因此删除保护、
确认和验收必须在开放入口前完整上线；不允许用回退脚本伪造已删除凭据。

## 11. 分阶段实施计划

### 11.1 推进顺序

阶段一先建立传统后台壳层和真实内容宽度基线；阶段一完成、自动化与三档浏览器验收通过、实施结果回填并进入当前基线后
必须停止。阶段二按照“二 A 统一视图模型与适配层 -> 二 B 统一工作台及原生编辑器迁移 -> 二 C 兼容回归与浏览器
验收”顺序推进。每个阶段完成后都必须回填实施结果并停止，只有用户再次明确确认才能启动下一阶段；不得并行实施，也
不得以后续阶段验收替代当前阶段验收。

阶段三按照“三 A 设置子路由与目录选择 -> 三 B1a Backend 创建契约 -> 三 B1b Backend 删除契约与引用保护 -> 三 B2
前端新增/删除闭环 -> 三 C 跨端兼容回归”顺序推进。三 A 先冻结导航、路由和详情选择行为；三 B1a 独立冻结创建契约；
三 B1b 在不开放前端入口的前提下完成破坏性删除及保护；三 B2 只在上述保护进入基线后开放前端入口；三 C 最后验证
引用保护、解析链、响应式和异常恢复。每个阶段继续执行独立授权和停止点，不因阶段一、二已经完成而自动连续实施。

### 11.2 阶段一：传统后台壳层

改动量等级：**中**。涉及 AppFrame 壳层、导航模型、三档响应式、沉浸模式兼容和整套前端回归，但不修改 Backend、
数据库或公开接口。

实施状态：**已完成（2026-08-07）**。

目标：一次完成普通应用壳层、桌面折叠、平板浮层、移动 Drawer、全局操作迁移和 Review 沉浸模式兼容，避免提交一个
只支持部分断点、会破坏现有页面的中间版本。

范围：

- 先将本文阶段一状态改为“实施中”，核对当前 `AppFrame`、样式和相关测试基线；
- 建立可测试的导航模型、路径选中和折叠偏好容错逻辑；
- 重组普通 AppFrame 为侧栏、全局顶栏和 Content；
- 完成桌面、平板、移动端交互及断点切换清理；
- 迁移现有版本、帮助、失败通知和调度队列触发器；
- 保持沉浸模式独占视口，并回归全部受影响页面；
- 补自动化测试、执行构建和浏览器验收，最后回填阶段一实施结果。

非目标：不处理第 8.2 节列出的事项，不实施阶段二，不借布局改造清理 App.jsx 中无关组件、替换 Ant Design/MUI、
调整 Command Center 视觉或恢复其它暂停专项。

验收：全部可见路由、桌面偏好、平板浮层、移动 Drawer、全局操作、沉浸模式恢复和三档布局符合第 4 至 7 节；相关
定向测试、全部前端测试和前端 build 通过。

授权边界与停止点：用户明确启动阶段一后，允许修改 React 壳层、纯展示模型、样式、对应测试和本文；不得修改 Backend、
数据库、公开 API、业务 schema、README、`docs/36` 或其它专项。完成后必须停止并汇报，不得自动进入阶段二、提交、
推送或部署。

### 11.3 阶段二 A：统一视图模型与适配层

改动量等级：**中**。需要梳理三份现有接口数据并建立稳定标识、字段映射、选择恢复和状态派生，但只新增纯前端模型与
测试，不切换设置页可见界面，也不改变任何保存行为。

实施状态：**已完成（2026-08-08）**。

前置条件：阶段一已完成、验收并进入当前基线；用户已明确授权启动阶段二 A。

目标：在不影响现有设置页的前提下，为 Agent Runtime 与 Standard Provider 建立统一、无 Key 明文、可独立测试的展示
模型和选择规则，先冻结阶段二后续 UI 所依赖的数据契约。

范围：

- 先将本文阶段二 A 状态改为“实施中”，核对阶段一基线、53 独立凭据边界、当前 Settings、Agent Settings 和 Provider
  返回结构；
- 建立 `AGENT:<runtimeType>`、`STANDARD:<providerCode>` 稳定行 ID 和统一目录字段映射；
- 实现 Agent 在前、Standard 在后的稳定排序、默认选择、原选中行刷新保持与缺失行回退；
- 派生当前/默认、配置完整性、Key 已配置、Worker 支持和更新时间等只读状态，不序列化或复制 Key 明文；
- 补纯函数与接口适配契约测试，确认现有设置页渲染、保存和测试流程完全不变；
- 回填阶段二 A 实施结果和阶段二 B 可依赖的模型契约。

非目标：不合并设置折叠项，不渲染双卡、目录或详情，不调整 Provider 保存语义，不实现 dirty 状态和响应式工作台；
第 8.2 节全部继续适用。

验收：第 12.3 节测试通过；统一行模型对完整、缺失和刷新数据均能稳定输出；产物不包含 Key 明文；现有设置页面行为和
全部前端测试、build 保持通过。

授权边界与停止点：只允许修改纯展示模型、对应测试和本文；如为接入模型必须修改现有设置页，只能添加不生效的导入或
适配入口，不得切换可见 UI。完成后必须停止并等待用户明确授权阶段二 B，不得自动提交、推送或部署。

实施结果（2026-08-08）：

- 新增 `frontend/src/reviewModelConnections.js`，显式映射两个 Agent Runtime 和全部有效 Standard Provider；Agent 行固定
  在前，Standard 行保持 Backend 目录顺序，重复或缺失 Provider Code 不进入目录；
- 目录行契约固定包含稳定 ID、Review 域、连接类型、Runtime/Provider 标识、名称、协议、Endpoint、模型、默认/当前、
  启用、Key 已配置、配置完整性、Worker 支持、配置状态和更新时间；不复制源对象、Key 明文或 Key 掩码；
- 默认选择契约固定为“原选中行仍存在则保持 -> Standard 默认 Provider -> Agent 当前 Runtime -> 首行”；缺失或非法
  Agent Runtime 回退到现有向后兼容默认 Runtime；
- 新模型未导入 `App.jsx`，现有“模型 Provider 配置”和“Agent Review”两个折叠项及 Provider 保存后设默认行为保持
  不变，界面迁移和保存职责拆分继续留在阶段二 B；
- 新增 10 项定向测试；全部前端测试 `178/178` 通过，`scripts/run-frontend.cmd build` 通过，仅有既有大 chunk 和构建
  耗时提示。

### 11.4 阶段二 B：统一工作台及原生编辑器迁移

改动量等级：**大**。同时涉及两个设置入口合并、双运行卡、目录/详情选择、三类原生编辑器、保存职责拆分、Key 操作、
Agent 异步测试、Provider 同步测试、dirty 防丢失及三档响应式，是阶段二主要用户可见改造。

实施状态：**已完成（2026-08-08）**。

前置条件：阶段二 A 已完成、验证并回填，统一展示模型进入当前基线；用户已明确授权启动阶段二 B。

目标：在复用阶段二 A 数据契约并保持 Backend 分域契约的前提下，一次交付可用的“模型连接与 Review 配置”工作台，
不向用户暴露只支持部分编辑器、部分保存动作或部分断点的中间界面。

范围：

- 先将本文阶段二 B 状态改为“实施中”，复核阶段二 A 契约和现有设置页请求生命周期；
- 合并“模型 Provider 配置”和“Agent Review”折叠项，完成顶部 Agent / Standard 运行卡；
- 接入统一目录、选择保持和三类原生详情编辑器，保留 Agent 预算独立区域；
- 将 Provider PUT 与 set-default 解耦，保留 Agent 双 Key、异步测试、Worker 状态、Standard Provider 同步测试和清除 Key；
- 实现 dirty 行切换确认、保存/清除后的状态清理及服务端数据恢复；
- 一次完成 `>=1200px`、`761px–1199px`、`<=760px` 三档布局；
- 补交互和源码契约测试，执行全部前端测试、build 与三档安全 mock 主路径验收；
- 回填阶段二 B 实施结果和待阶段二 C 深度回归的风险清单。

非目标：不新增 Backend、数据库、API、连接增删、测试结果持久化、真正共享连接池、Standard 独立开关或静态 fallback
选择；不修改项目、项目组、Profile 的 Provider 覆盖和解析优先级。

验收：第 9.2 节的双卡、目录、详情、原生保存/测试、默认选择、dirty 状态和响应式契约全部可用；Agent 与 Standard
请求、Key 和测试流程无串线；第 12.4 节测试和主路径浏览器验收、全部前端测试及 build 通过。

授权边界与停止点：只允许修改设置页 React、阶段二 A 展示模型、样式、对应测试和本文；不得修改 Backend、数据库、
公开 API、53 的凭据/运行契约、README 或其它专项。完成后必须停止并等待用户明确授权阶段二 C；不得自动提交、推送、
部署、请求真实 Provider 或执行真实 Agent Review。

实施结果（2026-08-08）：

- 设置页可见入口已合并为“模型连接与 Review 配置”，顶部提供 Agent / Standard 两张运行配置卡，下方以统一目录切换
  Agent 默认 Runtime、Agent Custom Runtime 和 Standard Provider 原生详情；Agent 执行预算保持独立区域；
- Agent 启用与运行时选择、当前 Runtime 详情、执行预算拆为三个独立保存动作；Agent 双 Key、清除 Key、Worker 状态和
  异步配置测试继续使用原 Agent Settings 契约，fallback 仅只读说明其继承 Standard 动态解析链；
- Standard Provider PUT 与 `set-default` 已解耦：详情保存不再隐式改变默认 Provider，顶部 Standard 卡单独保存默认
  连接；同步联通性测试和 Key 清除仍使用原 Provider 接口，不新增独立 Standard 开关；
- 目录选择支持刷新保持、Standard 默认优先的初始选择、键盘 Enter / Space 操作和 dirty 切换确认；确认丢弃后恢复最近
  服务端草稿，保存或清除成功后清理 dirty 状态；
- 三档布局已经落地：桌面为双卡加目录/详情双栏，平板保留双卡并将目录/详情上下排列，移动端全部单列、精简目录列并
  保持 44px 操作命中区；页面本身无横向溢出，宽表仅在目录内部滚动；
- 新增仅监听本机的安全 mock `frontend/tests/fixtures/docs54-settings-mock-server.mjs`，使用固定无效域名和内存数据完成浏览器验收，未
  携带真实 Key、源码或发起外部模型请求；
- 自动化验证通过：阶段二 A/B 及相关设置定向测试 `22/22`，全部前端测试 `183/183`，
  `node --check frontend/tests/fixtures/docs54-settings-mock-server.mjs` 和 `scripts/run-frontend.ps1 build` 均通过；构建仅有既有大
  chunk 提示；
- 安全 mock 浏览器主路径在 `1440px`、`1024px`、`390px` 通过，覆盖初始默认选择、三类详情切换、Provider 保存与设
  默认解耦、dirty 取消/丢弃、Custom Runtime 保存与测试，以及三档响应式和横向溢出检查。

待阶段二 C 深度回归的风险清单：

- 当前源码中旧 Agent / Provider 折叠项 JSX 已从可见项排除但尚未机械删除；阶段二 C 应在完整回归保护下清理不可达兼容
  块，并确认不误删仍由统一工作台复用的状态和请求逻辑；
- 阶段二 B 浏览器验收使用立即返回 `SUCCESS` 的安全 Agent mock，尚未在浏览器逐步演练 `QUEUED`、`RUNNING`、失败和
  超时轮询分支；现有自动化契约继续覆盖基础轮询逻辑；
- 三份设置读取任一失败、保存失败和 Key 清除失败等完整异常矩阵尚未逐项浏览器验收，留待阶段二 C 按第 12.5 节执行；
- 全局开关和项目、项目组、Profile Provider 覆盖未改变代码契约，但仍需在阶段二 C 做完整兼容回归，确认统一工作台
  没有引入间接解析优先级回归。

### 11.5 阶段二 C：兼容回归与浏览器验收

改动量等级：**中**。不再新增产品能力，主要覆盖多配置域兼容、异常分支、三档浏览器矩阵和必要回归修复；受 Agent
异步状态、Worker/预算/fallback 语义及 Provider 覆盖关系影响，验证面仍为中等。

实施状态：**已完成（2026-08-08）**。

前置条件：阶段二 B 已完成并通过主路径验收，风险清单已经回填；用户已明确授权启动阶段二 C。

目标：证明统一工作台没有改变 Agent / Standard 原生执行、安全和覆盖契约，完成阶段二的全量回归、三档安全 mock
浏览器验收和必要修复，使统一工作台进入当前基线。

范围：

- 先将本文阶段二 C 状态改为“实施中”，以阶段二 B 风险清单建立回归矩阵；
- 覆盖 Agent 独立双 Key、异步轮询、Worker 能力、执行预算、动态 Standard fallback 和异常恢复；
- 覆盖全局开关及项目、项目组、Profile Provider 覆盖语义，验证 Provider 保存与设默认完全解耦；
- 使用安全 mock 验证测试成功/失败、配置不完整、Worker 不支持、Key 掩码/清除和 dirty 取消/丢弃；
- 执行全部前端测试、build，以及 `1440px`、`1024px`、`390px` 完整浏览器验收；
- 只修复本阶段验收发现的阶段二回归问题，回填最终实施结果、验证证据和真实遗留风险。

非目标：不继续调整工作台信息架构或视觉风格，不新增阶段二 B 范围外能力；第 8.2 节全部继续适用。

验收：第 12.5 节兼容和浏览器矩阵全部通过，各断点无横向溢出、遮挡或双滚动；不产生真实模型请求、源码外发或模型
费用；阶段二实施结果完整回填。

授权边界与停止点：只允许修改阶段二工作台前端代码、样式、对应测试和本文，且修改必须用于修复已识别的验收问题；
不得扩展产品范围。完成后必须停止并汇报“改了什么、为什么、如何验证”和遗留风险；不得自动提交、推送、部署或继续
建设真正共享连接池。

实施结果（2026-08-08）：

- 删除阶段二 B 为降低切换风险暂留、但已不再渲染的 Agent / Provider 旧折叠项 JSX，并同步移除仅由旧入口使用的队列
  派生和校验变量；统一工作台成为设置页唯一模型连接配置入口；
- 将旧设置卡片源码契约测试迁移到统一工作台标题和 `Alert title` 契约，补充旧入口彻底消失、Agent 轮询终态与清理、
  周边设置域保留和读取失败重试等回归断言；
- 扩展本机安全 mock 的固定场景，覆盖 Agent 异步成功/失败、不完整 Custom Runtime、Worker 不支持、Provider 测试
  失败、Settings / Agent Settings / Provider 三类读取失败和保存失败；场景切换只作用于内存数据，不访问外部网络；
- 浏览器异常矩阵发现设置读取失败只有错误信息、缺少页面内恢复动作，已为设置页错误 Alert 增加复用现有 `load()` 的
  “重试”按钮；三类读取恢复均保持整页一致性，不在部分数据失败时开放混合版本编辑；
- 自动化最终验证通过：`node --check frontend/tests/fixtures/docs54-settings-mock-server.mjs`、全部前端测试 `187/187` 和
  `scripts/run-frontend.cmd build` 均通过；构建仅有既有大 chunk 提示；
- `1440px` 浏览器验收覆盖双卡等宽、目录/详情双栏、Standard 默认初选、三类详情、dirty 取消/丢弃、Agent 与
  Standard Key 独立清除、Provider 保存与设默认解耦；页面无横向溢出；
- `1024px` 验证 `72px` 图标侧栏、双卡并排、目录/详情上下排列以及不完整 Runtime、Worker 不支持和读取失败状态；
  `390px` 验证无固定侧栏、卡片单列、目录精简为“连接 / 所属 Review / 状态”、44px 操作命中区和键盘选中；两档均无
  页面横向溢出；
- 安全 mock 完整异常验收覆盖 Agent `QUEUED -> RUNNING -> SUCCESS / FAILED`、Provider 同步失败、三份读取失败后的
  页面内重试恢复、保存失败后 dirty 保留；未使用真实 Key、源码或真实 Provider，没有产生模型费用；
- 浏览器未发现产品运行异常；控制台仅保留工作台改造前已存在的 Ant Design `InputNumber addonAfter` 和其它 Drawer
  `width` 弃用提示，不影响本专项行为，未在阶段二 C 扩大范围处理。

最终遗留边界：

- 本专项没有执行真实 Agent Worker 或 Provider 联通请求；真实环境连通性继续由现有配置测试能力和有权限的运维验收
  负责，不能用安全 mock 结果推断外部服务可用性；
- 设置页仍由单次一致性加载聚合多个配置域；任一核心读取失败时整页进入错误态并允许重试，不提供部分数据编辑，这是
  为避免跨版本草稿混写而保留的现有安全边界；
- 既有 Ant Design 弃用提示和 `App.jsx` 体量治理不属于 54 的产品目标，后续如需处理应单独立项，不在本阶段顺带重构。

### 11.6 阶段三 A：设置子路由与目录直接选择

改动量等级：**中**。涉及壳层层级导航、四个设置子路由、设置模块渲染方式、跨路由 dirty 保护和三档响应式，但仅修改
前端，不改变公开 API、数据库或 Review 执行契约。

实施状态：**已完成（2026-08-08）**。

前置条件：阶段二 C 已完成并进入当前基线；用户已明确授权启动阶段三 A。

目标：先完成设置一级/二级导航、子路由和连接目录整行选择，使信息架构与参考图一致，并在没有增删接口的中间版本中
保持全部现有配置能力可用。

范围：

- 将本文阶段三 A 标记为“实施中”，复核阶段一壳层和阶段二工作台基线；
- 扩展可测试导航模型，加入四个设置二级菜单、父子路由高亮和 `/settings` 兼容重定向；
- 将四个 Collapse 模块改为按子路由单模块渲染，保持现有 Settings 控制器、请求和保存契约；
- 为当前模块建立统一 dirty 注册与离开保护，覆盖二级/一级菜单、前进后退、刷新关闭和焦点恢复；
- 删除目录“详情”按钮，保留整行鼠标/键盘选择、选中态、dirty 切换确认和移动端可点击高度；
- 补导航、路由、可访问性、dirty 和响应式测试，执行全部前端测试、build 与三档浏览器验收；
- 回填三 A 实施结果和三 B1a 接入点后停止。

非目标：不实现 Provider POST / DELETE，不显示不可用的新增/删除按钮，不调整 Provider DTO、Backend、数据库、Agent
Runtime 或现有配置字段；不新增参考图中的其它二级菜单。

验收：四个子路由均可直接访问、刷新、前进后退且父子菜单高亮正确；各模块行为与阶段二一致；目录不再出现“详情”
按钮，点击行或 Enter / Space 均展示详情；dirty 导航不会静默丢失；三档无横向溢出、遮挡或双滚动。

授权边界与停止点：只允许修改前端导航模型、Router、Settings 展示结构、样式、测试和本文。完成后必须停止并等待用户
明确授权阶段三 B1a；不得自动修改 Backend、提交、推送或部署。

实施结果：

- 新增四个设置子路由，`/settings` 使用 `replace` 进入 `/settings/model-connections`；桌面展开侧栏默认展开“设置”分组，
  桌面折叠、平板浮层和移动 Drawer 继续复用阶段一导航容器并正确高亮父子菜单；
- 四个原 Collapse 模块改为共享 Settings 控制器下的按路由单模块渲染，现有加载、保存、Key、测试和 Provider 覆盖请求
  保持不变；
- 设置草稿按模块注册 dirty 状态，壳层导航使用统一确认 Modal，刷新/关闭使用 `beforeunload`，浏览器前进后退使用在
  Router 初始化前注册的 history 守卫；取消离开保留路由、草稿和焦点，确认后恢复服务端数据；
- 模型连接目录移除“详情”按钮和操作列，整行继续支持鼠标、Enter / Space、`aria-selected` 与选中高亮；阶段三 B1a/B1b/B2
  尚未显示新增或删除入口；
- 新增 `settingsNavigation` 路由与 dirty 模型测试，并更新壳层、统一工作台和设置布局契约；全部前端测试 `190/190`
  通过，`scripts/run-frontend.cmd build` 通过，只有既有大 chunk 提示；
- 安全 mock 浏览器验收通过：`1440px` 验证四个二级菜单、整行鼠标/键盘选择和 dirty 取消/确认；`1024px` 验证
  `72px` 图标栏、`224px` 浮层及目录/详情上下排列；`390px` 验证无固定侧栏、嵌套 Drawer 选中后关闭及目录精简为
  “连接 / 所属 Review / 状态”。三档均无页面横向溢出，四个子路由可直接访问、刷新、前进和后退；
- 未修改 Backend、数据库、公开 API、schema、README 或 `docs/36`，未提交、推送或部署；当时阶段三 B1a/B1b 的
  Provider POST / DELETE 和阶段三 B2 的新增/删除 UI 均保持未实施。

### 11.7 阶段三 B1a：Backend 创建契约

改动量等级：**中**。新增一个边界明确的 Provider POST 契约，涉及 Python DTO、API、Service、Repository 和定向契约
测试，但不包含删除、跨域引用查询、数据库迁移、前端入口或 Review 执行链修改。

实施状态：**已完成（2026-08-09）**。

前置条件：阶段三 A 已完成、验证并进入当前基线；用户已明确授权启动原阶段三 B1，本文按新改动量规则先完成拆分。

目标：按第 9.3.3 节完成可独立验证的自定义 Standard Provider 创建契约，冻结 B2 新增界面的服务端输入、响应与错误码。

范围：新增明确创建 DTO 和 POST API；强制 Code 规范化、协议白名单、字段长度、启用完整性、`built_in=false` 与稳定排序；
复用现有表、Key 掩码和超时边界；处理重复 Code 并保证不改变平台默认或覆盖配置；补充 Backend contract / unit 测试并
执行受影响 Python 测试。

非目标：不实现 DELETE 或任何引用保护；不修改 React、安全 mock、数据库 schema、Agent Runtime、Provider 更新语义、
Review 解析优先级和历史快照。

验收：合法自定义 Provider 可创建并按现有响应格式脱敏；重复、非法或配置不完整的启用请求确定性失败；创建不改变平台
默认且不触发真实 Provider；受影响 Backend 测试通过。

授权边界与停止点：只允许修改 Python Provider DTO / API / Service / Repository、Backend 测试和本文。完成后必须停止，
等待用户明确授权阶段三 B1b；不得实现删除、修改前端、自动提交、推送或部署。

实施结果：

- 新增 `CreateProviderRequest` 明确 DTO 与 `POST /api/code-quality-review-providers`，实现 Code 大写规范化、协议白名单、
  字段长度和超时边界，以及启用状态下 Endpoint、模型、Key 完整性校验；
- Repository 强制 `built_in=false`，按当前最大 `sort_order + 10` 排序，响应复用现有 Key 掩码且只返回新建 Provider；
  创建不修改平台默认 Provider，也不触发联通性测试或 Review 执行；
- 重复 Code 的预检查和数据库唯一约束竞态均转换为 `409 PROVIDER_ALREADY_EXISTS`；未实现 DELETE 或引用保护；
- 新建契约定向测试 `5/5` 通过，既有 Provider 更新、目录、同步测试兼容测试 `6/6` 通过，本次文件定向 Ruff 通过；
- 完整 `test_code_quality_api_contract.py` 为 `82/85`，失败的 3 项在单独运行时仍失败，分别是既有 fix-preview 404、双
  Provider mock 仍选中 OPENAI、push debounce 首次被拒绝，均不调用创建接口，作为阶段三 C 的既有基线问题记录；
- 未修改 React、数据库 schema、README 或 `docs/36`，未提交、推送、部署或请求真实 Provider。

### 11.8 阶段三 B1b：Backend 删除契约与引用保护

改动量等级：**中**。只新增一个 Provider DELETE 契约及现有五类配置的引用检查，涉及显著的破坏性操作验证，但不再
修改创建契约、前端、数据库 schema 或跨服务执行链。

实施状态：**已完成（2026-08-09）**。

前置条件：阶段三 B1a 已完成、创建契约测试通过并进入当前基线；用户已明确授权启动阶段三 B1b。

目标：按第 9.3.4 节完成自定义 Standard Provider 删除契约，使后续前端删除入口建立在确定性的服务端安全边界之上。

范围：新增 DELETE API、Service 与 Repository 事务；实现内置、平台默认及五类配置引用保护；历史 Review 快照不阻塞、
不级联；补充不存在、重复删除和并发边界测试，并执行受影响 Python 测试。

非目标：不修改 POST 创建契约、React 或安全 mock；不新增数据库表或迁移，不动态增删 Agent Runtime，不建设软删除、
回收站或历史快照清理。

验收：内置、默认和五类在用 Provider 均返回计划错误码；无引用自定义 Provider 可删除；不存在、重复删除及并发冲突
具有确定性结果；受影响 Backend 测试通过。

授权边界与停止点：只允许修改 Python Provider API / Service / Repository、Backend 测试和本文。完成后必须停止，等待
用户明确授权阶段三 B2；不得修改前端、自动提交、推送、部署、调用真实 Provider 或产生模型费用。

实施结果：

- 新增 `DELETE /api/code-quality-review-providers/{providerCode}`，Provider Code 统一按大写解析；不存在返回
  `404 RESOURCE_NOT_FOUND`，成功返回 `{"providerCode":"...","deleted":true}`；
- 删除事务先锁定 Provider 和平台设置记录，再对 Profile、项目、项目组、项目组模型、端类型配置执行锁定读取；内置、
  默认和在用 Provider 分别返回 `PROVIDER_BUILT_IN`、`PROVIDER_IS_DEFAULT`、`PROVIDER_IN_USE`，引用错误信息包含配置域
  与数量；
- 无引用自定义 Provider 永久删除，重复删除确定性返回 404；历史 `CodeQualityReviewResult` 快照不阻塞、不级联且删除后
  保持 Provider/模型文本；
- B1a/B1b 生命周期定向测试 `9/9` 通过，既有 Provider 更新、目录与同步测试兼容测试 `6/6` 通过，本次文件定向 Ruff
  通过；完整 contract 文件 `86/89`，失败集合与 B1a 相同且没有新增失败；
- 未修改 React、数据库 schema、README 或 `docs/36`，未提交、推送、部署或请求真实 Provider。

### 11.9 阶段三 B2：前端新增与删除闭环

改动量等级：**中**。需要跨统一工作台、目录/详情、Modal、dirty 守卫、安全 mock 和三档响应式实现显著交互，但以阶段
三 B1a/B1b 已冻结的 API 为前提，不再新增公开契约、数据库迁移或跨服务改造。

实施状态：**已完成（2026-08-09）**。

前置条件：阶段三 B1a、阶段三 B1b 均已完成、Backend 契约测试通过并进入当前基线；用户已明确授权启动阶段三 B2。

目标：在 Backend 保护已就绪的前提下开放自定义 Standard Provider 新增/删除界面，形成完整但不扩大数据模型的前端
生命周期闭环。

范围：

- 将本文阶段三 B2 标记为“实施中”，以 B1a/B1b 的 DTO、响应和错误码作为唯一接入契约；
- 增加“新增模型连接”Modal，处理字段校验、Key 内存边界、失败草稿保持、成功刷新并选中新行；
- 增加桌面目录删除操作和移动端详情危险区，Agent Runtime、内置及默认 Provider 不出现可执行删除入口；
- 实现行操作事件隔离、dirty 处理、完整 Provider Code 二次确认、删除成功选择回退及 409 失败状态保持；
- 更新阶段二安全 mock，补前端模型、交互、可访问性与响应式测试；
- 执行全部前端测试和 `scripts/run-frontend.cmd build`，以自动化测试冻结新增/删除主路径、响应式展示条件和安全 mock；
- 回填 B2 结果、待浏览器验证项及阶段三 C 风险清单后停止；B2 不单独执行浏览器验收，三档浏览器矩阵统一并入阶段三 C。

非目标：不主动修改 B1a/B1b Backend 契约，不新增 Provider 协议、数据库表或迁移，不动态增删 Agent Runtime，不自动设默认、
自动测试或迁移现有覆盖关系，不建设软删除、分页或持久化最近测试/延迟。

验收：新增成功后目录刷新并选中新行且不自动默认/测试；删除入口只出现在允许范围，确认和 dirty 处理完整；成功删除按
计划回退选择，Backend 409 保留当前详情并展示真实原因；响应式分支和安全 mock 自动化测试、全部前端测试与 build
通过。真实三档布局、无横向溢出和完整交互视觉验收保留到阶段三 C，不作为 B2 越权连续实施的理由。

授权边界与停止点：只允许修改 React 工作台、前端适配层、样式、安全 mock、前端测试和本文；若发现 B1a/B1b 契约缺陷，应
停止并回到 Backend 专项修正，不得在前端绕过。完成后必须停止并等待用户明确授权阶段三 C；不得自动提交、推送、部署
或请求真实 Provider。

实施结果：

- 新增自定义 Standard Provider Modal，覆盖 Code 大写规范化、三类协议、字段/超时/启用完整性校验、内存 Key 草稿、
  失败保持和成功刷新；成功后选中新行，不调用 set-default 或联通性测试；
- 桌面/平板目录新增仅对自定义 Provider 可见的删除操作，内置不展示、默认自定义 Provider 禁用并说明原因；移动端详情
  增加危险操作区；删除前复用连接 dirty 丢弃确认并要求输入完整 Provider Code；
- 删除成功后重新读取目录：删除当前行按“Standard 默认 -> Agent 当前 Runtime -> 首行”回退，删除其它行保持当前选择；
  Backend 409 或其它失败只保留在确认 Modal，目录、详情和选择不变；
- 创建草稿纳入设置模块 dirty 导航保护；安全 mock 新增 POST、DELETE、重复、内置、默认、在用和普通失败响应，仍只在
  `127.0.0.1` 使用虚构数据且不请求模型；
- 新增纯生命周期模型和测试；定向测试 `28/28`、全部前端测试 `196/196`、`scripts/run-frontend.cmd build` 通过。构建仅有
  既有大 chunk 与插件耗时提示；
- 按调整后的计划未执行浏览器验收。阶段三 C 必须在 `1440px`、`1024px`、`390px` 统一验证新增 Modal、桌面/移动删除、
  Code 确认、dirty、成功回退、409 保持、软键盘和无横向溢出；
- 未主动修改 B1a/B1b Backend 契约、数据库 schema、README 或 `docs/36`，未提交、推送、部署或请求真实 Provider。

### 11.10 阶段三 C：生命周期与设置导航完整回归

改动量等级：**中**。不新增产品能力，重点覆盖跨模块引用保护、设置子路由、破坏性操作异常、响应式和必要回归修复；
验证横跨前后端，但不再新增接口或迁移。

实施状态：**已完成（2026-08-09）**。

前置条件：阶段三 B1a、阶段三 B1b、阶段三 B2 均已完成并通过各自主路径验收，风险清单已回填；用户已明确授权启动
阶段三 C。

目标：证明设置二级导航和 Provider 生命周期没有破坏 Agent / Standard 分域、默认与覆盖解析、历史记录、安全凭据及
三档后台壳层，使阶段三进入当前基线；同时一次完成阶段三 B2 新增/删除界面与阶段三 C 完整回归的浏览器验收。

范围：

- 将本文阶段三 C 标记为“实施中”，按三 B1a、三 B1b、三 B2 风险清单建立完整回归矩阵；
- 覆盖创建并发冲突、删除并发/重复请求、内置/默认/五类引用保护、删除后目录选择和历史结果展示；
- 回归 Agent 双 Runtime、Standard 默认、项目/项目组/Profile/端类型覆盖和动态 fallback；
- 回归四个设置子路由的直接访问、前进后退、折叠/浮层/Drawer、dirty 取消/丢弃和焦点恢复；
- 执行 `scripts/run-backend.cmd test`、全部前端测试、build，以及 `1440px`、`1024px`、`390px` 安全 mock 浏览器矩阵；
- 浏览器矩阵同时覆盖 B2 的新增 Modal、目录/详情删除入口、Code 二次确认、成功选择回退、409 阻塞和错误恢复，不在
  B2 与 C 之间重复启动同一套浏览器环境；
- 只修复阶段三引入的回归，回填最终结果和真实遗留风险。

非目标：不扩展 Provider 协议、不接入真实模型、不优化 App.jsx 体量或处理无关 Ant Design 弃用提示，不继续建设共享
Provider / Agent 连接池。

验收：第 12.10 节全部通过；新增、删除和路由行为在三档可用，无静默草稿丢失、引用悬空、Key 泄露、页面横向溢出或
历史数据破坏；安全 mock 不产生外部请求或模型费用。

授权边界与停止点：只允许为阶段三验收修复前后端、样式、测试、安全 mock 和本文，不得扩大产品范围。完成后必须停止
并汇报“改了什么、为什么、如何验证”和遗留风险；不得自动提交、推送或部署。

实施结果：

- 阶段三实现不需要追加代码修复；生命周期定向契约在全量套件中继续通过，未发现阶段三引入的新失败；
- `scripts/run-backend.cmd test` 实际结果为 `529 passed / 4 failed / 1 skipped`。其中
  `test_code_quality_api_contract.py` 的 fix preview 旧索引、OpenAI/Anthropic mock Provider 切换、push debounce 三项与
  B1a/B1b 记录的既有基线一致；`test_projects_api_contract.py` 的新项目 GENERAL Profile 状态断言单独运行仍稳定失败，
  该用例不经过阶段三 Provider 创建/删除接口，作为现有仓库基线记录，不在本阶段扩修；
- 全部前端测试 `196/196` 通过，`scripts/run-frontend.cmd build` 通过；构建仅保留既有大 chunk 提示；
- `1440px` 安全 mock 验证新增校验、创建后选中、不自动设默认/测试、完整 Code 删除确认、删除后选择回退、409 保持
  目录与详情、dirty 取消/丢弃以及无页面横向溢出；
- `1024px` 验证 `72px` 图标栏、`224px` 设置浮层、双运行卡并排、目录/详情上下排列、子路由选择后浮层关闭以及无
  页面横向溢出；
- `390px` 验证无固定侧栏、两张运行卡单列、精简目录整行选择、详情危险操作区、完整 Code 输入启用确认、`280px`
  Drawer（小于 `88vw`）、子路由选择后关闭以及无页面横向溢出；
- 浏览器控制台只出现本文已列为非目标的既有 Ant Design 弃用警告；验收全程使用本机固定安全 mock 和虚构 Key，未请求
  真实 Provider、未执行 Agent Review、未产生模型费用；本次启动的验收服务已停止并释放端口。

## 12. 测试与验收矩阵

所有实施阶段至少执行：

```text
node --test frontend/tests/*.test.mjs
scripts/run-frontend.cmd build
```

优先运行当期定向测试，再执行全部前端测试。若仓库脚本失败且必须进入 `frontend/` 执行底层命令，应按 `AGENTS.md`
记录原因。

### 12.1 阶段一自动化测试

- 导航模型：根路由精确匹配、任务详情、质量治理子路由、验收详情、回放详情、未知路由和功能开关；
- 偏好状态：缺失、`true`、`false`、非法值、读取异常和写入异常；
- 壳层状态：桌面展开/折叠、平板临时展开、移动 Drawer、断点切换和沉浸模式进入/退出；
- 操作回归：失败通知数量与危险态、队列 Badge、两个 Modal 打开、任务跳转和取消动作保持；
- 无虚假身份、无新增远程资源、无隐藏质量治理入口泄漏；
- 现有 Review 沉浸模式和 Command Center 信息架构测试继续通过。

### 12.2 阶段一浏览器验收

| 视口 | 重点检查 |
| --- | --- |
| `1440px` | 默认 224px、折叠 72px、偏好刷新恢复、全局顶栏、宽表格和运行总览 |
| `1024px` | 72px 图标栏、224px 临时浮层、外部点击/Escape/焦点恢复、内容宽度不变化 |
| `390px` | 无固定侧栏、Drawer 宽度、四项全局操作、菜单导航、无横向溢出 |

三档均检查运行总览、任务列表、任务详情、设置、版本更新、接入帮助、失败通知和调度队列；任务详情额外检查普通结果模式
与 Review 沉浸模式互相切换。不得连接或触发真实 Agent Review。

### 12.3 阶段二 A 自动化测试

- 统一展示模型：两类稳定行 ID、字段映射、Agent/Standard 分域标签和稳定排序；
- 选择规则：Standard 默认、Agent 当前 Runtime、首行回退、原选中行刷新保持和选中行消失后的重新选择；
- 状态派生：默认/当前、配置完整性、Key 已配置、Worker 支持、更新时间和缺失数据降级；
- 安全边界：序列化展示模型不含 Key 明文，不推断持久化延迟、项目使用范围或固定 Agent fallback；
- 无行为切换：现有两个设置折叠项、保存、测试和 Key 操作的源码契约继续通过。

### 12.4 阶段二 B 自动化与主路径验收

自动化测试：

- 顶部运行卡：Agent 启用/运行时/Custom 推理强度和 Standard 默认 Provider 的独立保存；
- 请求职责：Provider PUT 不调用 set-default，顶部 Standard 保存只调用 set-default；Agent 继续使用 Agent Settings；
- 原生详情：Agent 双 Key 保留/清除、Custom URL/TLS/Worker 校验、Provider 草稿测试和会话内结果；
- dirty 行切换：无修改直接切换，有修改时取消保持、确认丢弃并恢复服务端值，保存后不再提示；
- UI 边界：不出现新增/删除、共享 Key、持久化延迟、虚假使用范围、Standard 独立开关或静态 fallback 选择。

主路径浏览器验收：

| 视口 | 重点检查 |
| --- | --- |
| `1440px` | 双运行卡、目录/详情双栏、默认连接保存、Agent 与 Standard 详情切换 |
| `1024px` | 双卡、目录和详情上下排列、侧栏有效宽度、dirty 确认 |
| `390px` | 单列卡片、精简目录、详情表单、44px 操作命中区、无页面横向溢出 |

### 12.5 阶段二 C 兼容回归与完整浏览器验收

- 兼容自动化：Agent 预算始终可见、独立双 Key、Worker 能力、动态 Standard fallback、全局开关和项目/项目组/Profile
  覆盖语义不变；
- 异常自动化：三份读取任一失败、Agent 异步排队/成功/失败、Provider 同步成功/失败、保存失败、Key 清除失败和刷新
  后选中恢复；
- 完整浏览器矩阵在 `1440px`、`1024px`、`390px` 分别覆盖双卡、目录选择、三类详情、错误状态、dirty 取消/丢弃、
  Key 掩码/清除、Agent 预算、折叠和无横向溢出；
- 回归设置页其它折叠项、全局开关及阶段一侧栏有效宽度，确认无页面遮挡、双滚动或壳层回归。

阶段二 B、二 C 的浏览器验收均使用无真实密钥、无源码和无模型费用的安全 mock。未经用户额外授权，不请求真实中转站
或 Provider，不执行真实 Agent Review。

### 12.6 阶段三 A 导航与目录交互验收

- 导航模型：四个子路由、`/settings` replace、父子高亮、未知设置子路由回退和其它一级菜单不变；
- 壳层交互：桌面展开/折叠、平板浮层、移动 Drawer 的二级菜单打开、选择、Escape / 外部点击和焦点恢复；
- 单模块渲染：每个子路由只显示对应模块，原四个设置模块的字段、保存、测试和错误恢复行为不变；
- dirty：二级切换、一级切换、前进后退、刷新/关闭分别覆盖取消、确认丢弃和保存后直接离开；
- 目录：无“详情”按钮；行点击、Enter / Space、`aria-selected`、选中保持和移动端 44px 命中区通过；
- 全部前端测试、build 和 `1440px` / `1024px` / `390px` 浏览器验收通过。

### 12.7 阶段三 B1a Backend 创建验收

- Backend 创建契约：Code 规范化/格式/唯一性、三类协议、字段长度、超时、启用完整性、`built_in=false`、排序和响应
  Key 脱敏；创建不改变平台默认；
- 运行受影响 Backend contract / unit 测试，不请求真实 Provider，不产生模型费用。

### 12.8 阶段三 B1b Backend 删除与引用保护验收

- Backend 删除契约：内置、默认、Profile、项目、项目组、项目组模型和端类型配置引用分别阻塞；无引用自定义 Provider
  成功删除；不存在、重复删除和并发冲突返回确定性错误；历史结果不级联；
- API 防绕过：Agent Runtime 与内置 Provider 不能通过构造请求删除，删除保护在同一事务中复核；
- 运行受影响 Backend contract / unit 测试，不请求真实 Provider，不产生模型费用。

### 12.9 阶段三 B2 前端生命周期验收

- 前端新增：Modal 校验、协议字段、Key 内存边界、失败草稿保持、成功刷新选中且不自动默认/测试；
- 前端删除：行操作不冒泡、移动危险区、dirty 处理、Code 二次确认、成功选择回退、409 错误展示和选择保持；
- Agent Runtime、内置和默认 Provider 永远不出现可执行删除入口，但前端隐藏不能代替 B1b 的 Backend 保护；
- 运行定向交互与响应式自动化测试、全部前端测试和 build；本阶段不单独启动浏览器验收环境；
- 将新增、删除、dirty、错误恢复和三档布局的待验项回填为阶段三 C 浏览器清单，完成后停止并等待 C 的独立授权。

### 12.10 阶段三 C 完整回归

- 执行 `scripts/run-backend.cmd test`，覆盖 Provider CRUD、配置引用、Review Provider 解析、历史结果和现有接口兼容；
- 执行全部 `frontend/tests/*.test.mjs` 与 `scripts/run-frontend.cmd build`；
- `1440px` 覆盖二级菜单常驻、目录/详情双栏、新增、删除、默认/引用阻塞、成功选择回退和 dirty 路由保护；
- `1024px` 覆盖图标栏设置浮层、目录/详情上下排列、新增 Modal、删除确认框、409 保持选择和错误恢复；
- `390px` 覆盖嵌套 Drawer、单模块页面、目录整行选择、详情危险区、Code 二次确认、软键盘下确认和无横向溢出；
- 回归 Agent 双 Runtime、Standard 默认、项目/项目组/Profile/端类型覆盖、动态 fallback 和历史任务展示；
- 浏览器只使用固定安全 mock 和虚构 Key，不执行真实 Provider 测试或 Agent Review，不产生模型费用。

阶段三浏览器验收继续使用无真实密钥、无源码和无模型费用的安全 mock；阶段三 B1a/B1b/C 的 Backend contract 测试只使用
测试数据库事务和虚构凭据，不请求外部模型服务。

阶段三 B2 与阶段三 C 只合并浏览器验收批次，不合并实施阶段、授权边界或停止点。B2 必须先完成自动化测试与 build、
回填风险并停止；只有用户再次明确授权 C 后，才能执行合并后的三档浏览器验收和完整回归。

## 13. 文档落地结果

- 2026-08-07：完成传统后台壳层可行性评估和阶段一计划，确认编号 54 未占用；
- 2026-08-07：确认 Agent 与 Standard 可采用“统一视图、分域保存”，回写阶段二运行卡、连接目录、原生详情、接口边界、
  响应式、测试和停止点；不建设真正共享连接池；
- 2026-08-07：根据改动量复核将原阶段二拆分为阶段二 A（统一视图模型与适配层，改动量中）、阶段二 B（统一工作台
  及原生编辑器迁移，改动量大）和阶段二 C（兼容回归与浏览器验收，改动量中）；三个阶段均设置独立授权边界和停止点；
- 2026-08-08：完成阶段二 A。建立 Agent/Standard 安全统一目录模型、稳定 ID、状态派生和选择恢复契约；没有接管现有
  设置页 UI、保存、Key 或测试流程，没有修改 Backend、数据库、API、schema、README 或 `docs/36`；
- 2026-08-08：完成阶段二 B。设置页已切换为统一 Review 模型配置工作台，完成双运行卡、统一目录、三类原生详情、
  Agent 分职责保存、Provider 保存与设默认解耦、dirty 防丢失和三档响应式；全部前端测试 `183/183`、build 与三档
  安全 mock 主路径验收通过；未修改 Backend、数据库、公开 API、schema、README 或 `docs/36`，当前停止并等待阶段
  二 C 明确授权；
- 2026-08-08：完成阶段二 C。清理不可达旧配置入口，补齐固定安全 mock 异步与异常矩阵，并为设置读取失败增加页内
  重试；全部前端测试 `187/187`、build 和 `1440px` / `1024px` / `390px` 完整浏览器验收通过，阶段二全部阶段完成；
- 2026-08-08：根据新增需求完成阶段三规划。确认设置四模块可映射为二级菜单，目录可移除“详情”按钮并改为整行选择；
  Provider 增删限定为自定义 Standard Provider，新增 POST / DELETE 契约并保护 Agent Runtime、内置、默认及五类在用
  Provider；拆分为阶段三 A（设置子路由与目录选择，改动量中）、三 B（Provider 生命周期，改动量大）、三 C（完整
  回归，改动量中）；其中阶段三 A 已于同日实施完成，三 B、三 C 仍未实施；
- 2026-08-08：完成阶段三 A。四个设置模块已成为“设置”一级菜单下的二级路由，旧 `/settings` 兼容重定向；连接目录
  改为整行鼠标/键盘选择并移除“详情”按钮；模块级 dirty 守卫覆盖壳层导航、浏览器历史和页面关闭；全部前端测试
  `190/190`、build 与 `1440px` / `1024px` / `390px` 安全 mock 浏览器验收通过；
- 2026-08-09：复核阶段三 B 改动量和安全发布顺序，确认需要拆分为阶段三 B1（Backend 生命周期契约与五类引用保护，
  改动量大）和阶段三 B2（前端新增/删除闭环，改动量中）。B1 必须先独立完成并进入基线，B2 才能开放破坏性界面；
  两阶段均未实施并设置独立授权与停止点，阶段三 C 顺延为第 11.9 / 12.9 节；
- 2026-08-09：根据新增“规划阶段不得保留大阶段”规则，将原阶段三 B1 继续拆分为 B1a（Backend 创建契约，改动量中）
  与 B1b（Backend 删除契约与引用保护，改动量中），B2 和 C 顺延；B1a 已完成明确 DTO、POST API、稳定排序、Key 脱敏、
  重复冲突处理和定向测试，当前停止并等待 B1b 明确授权；
- 2026-08-09：完成阶段三 B1b。新增自定义 Standard Provider DELETE 契约、内置/默认保护、五类配置引用锁定检查和
  历史快照非阻塞语义；B1a/B1b 生命周期测试 `9/9`、既有 Provider 兼容测试 `6/6`、定向 Ruff 通过，完整 contract
  文件保持同一 3 项非生命周期失败；当前停止并等待 B2 明确授权；
- 2026-08-09：确认阶段三 B2 与阶段三 C 可以合并浏览器验收批次。B2 仍独立完成前端实现、自动化测试、build、风险
  清单并停止；C 经独立授权后统一执行 `1440px` / `1024px` / `390px` 浏览器矩阵，同时验收 B2 生命周期界面和 C 的
  跨端完整回归。两阶段改动量等级均保持“中”，实施授权和停止点不合并；
- 2026-08-09：完成阶段三 B2。前端已开放自定义 Standard Provider 新增 Modal、桌面目录删除、移动详情危险区、完整
  Code 确认、dirty 处理、成功选择回退和 409 状态保持；安全 mock 已支持生命周期场景；定向测试 `28/28`、全部前端
  测试 `196/196`、build 通过。按计划未执行浏览器验收，当前停止并等待阶段三 C 明确授权；
- 2026-08-09：完成阶段三 C。全量 Backend 为 `529 passed / 4 failed / 1 skipped`，4 项均确认为不经过阶段三生命周期
  接口的现有基线；全部前端测试 `196/196` 和 build 通过；`1440px`、`1024px`、`390px` 安全 mock 联合浏览器验收
  覆盖新增、删除、引用阻塞、dirty、设置子路由、响应式和无横向溢出并通过。54 专项全部阶段完成；
- 2026-08-07：完成阶段一传统后台壳层。新增可测试的导航、详情路由选中、断点和侧栏偏好模型；将普通 `AppFrame`
  调整为桌面 `224px/72px` 侧栏、平板 `72px` 图标栏加 `224px` 浮层、移动端最大 `88vw` Drawer，以及 `56px`
  吸顶全局顶栏；帮助、版本、失败通知和调度队列继续复用原行为；
- 阶段一保持质量治理功能开关、路由、通知/队列请求、Modal、Backend、数据库和业务 schema 不变；没有新增用户身份、
  远程资源、前端依赖、README 或 `docs/36` 修改；
- 阶段一自动化验证：`node --test frontend/tests/*.test.mjs` 通过，共 `168/168`；
  `scripts/run-frontend.cmd build` 通过。构建只有既有大 chunk 提示，无编译错误；
- 阶段一浏览器验收：`1440px` 验证默认展开、折叠和刷新恢复，`1024px` 验证固定图标栏、浮层尺寸、Escape 关闭和
  焦点恢复，`390px` 验证无固定侧栏、`280px` Drawer、选中后关闭和四项全局操作；运行总览、任务、设置、版本更新、
  接入帮助及历史任务详情均无页面横向溢出，失败通知和调度队列 Modal 可正常打开；
- 浏览器验收期间发现任务列表宽表格会在 `390px` 撑宽文档，已改为表格内部横向滚动；阶段一新增 Drawer 的弃用属性
  警告也已修正，最终复核无新增浏览器 warning/error；
- 当前环境没有运行中的 ReviewTask，未在浏览器产生真实 Review 或模型费用来制造沉浸态。沉浸模式的进入/退出、离开
  详情恢复普通壳层和侧栏/顶栏同时不渲染，继续由 `reviewImmersivePresentation` 测试及新增 AppFrame 壳层源码契约覆盖；
- 阶段三 A 没有修改 Backend、数据库、README 或 `docs/36`；阶段三 B1a/B1b 没有修改前端或数据库 schema；阶段三 B2
  没有修改 Backend 或数据库 schema；阶段三 C 仅回填验收结果，没有追加产品代码。上述改动均未提交、推送或部署。
  54 专项当前停止；真正共享连接池、多 Agent Runtime、其它设置页面或 Ant Design 依赖治理仍需另行立项。

## 14. 模型连接工作台视觉收敛

### 14.1 单阶段实施边界

改动量等级：**中**。本轮只调整一个设置页面，但同时涉及页面级 Agent Review 开关、双运行摘要卡、目录/详情工作台、
三类原生编辑器和三档响应式，需要完整验证既有保存职责和 dirty 防丢失行为。

实施状态：**已完成（2026-08-13）**。

目标：在不改变 Agent Runtime、Standard Provider、Key、测试和预算契约的前提下，将模型连接工作台收敛为更清晰的
“页面标题与全局操作 -> 双运行摘要 -> 连接目录与详情 -> Agent 执行预算”信息层级。

范围：

- 将“新增模型连接”移动到页面标题区，并使用更紧凑的双运行摘要卡；
- 将 Agent Review 启停从当前连接详情移出，作为页面级独立全局开关，继续复用 Agent Settings PUT；
- 收敛目录标题、列宽、行高、选中态和目录/详情桌面比例，保留现有删除入口、整行选择和键盘交互；
- 将连接详情调整为紧凑标题、摘要标签、弱边框编辑字段和底部操作区的独立面板；
- 保留 Agent 执行预算的位置、字段和独立保存行为，并完成 `1440px`、`1024px`、`390px` 三档适配。

非目标：不新增“运行状态”页签，不改变配置状态定义或测试结果生命周期，不将删除改成统一三点菜单，不修改 Backend、
数据库、公开 API、schema、Provider 默认解析、Agent fallback、Key 安全边界或 Agent 执行预算契约。

验收：Agent 全局开关与连接详情互不串线；三类连接的编辑、保存、测试、Key 清除、设为当前和受保护删除继续可用；
目录 dirty 切换保护与键盘操作不回归；定向测试、全部前端测试、前端 build 和三档浏览器验收通过，页面无新增横向溢出。

授权边界与停止点：只允许修改本文、模型连接设置页 React、样式和对应前端测试；不得调用真实 Provider、执行真实
Agent Review、修改 Backend、数据库、README 或 `docs/36`。本阶段完成验证并回填结果后停止，不自动提交、推送或部署。

### 14.2 实施结果

- 页面标题区已承载独立的 Agent Review 全局开关和“新增模型连接”入口；连接详情不再重复渲染全局开关，原 Agent
  Settings PUT、加密可用性和 dirty 禁用边界保持不变；
- 双运行摘要卡已改为 Agent / Standard 独立图标、紧凑状态和只读连接摘要；目录完成标题、列宽、密度、选中态、加载
  收尾文案和桌面占比收敛，仍使用原删除按钮、整行鼠标/键盘选择和配置状态语义；
- Agent Runtime 与 Standard Provider 详情已统一为无装饰竖线和副描述的紧凑标题、摘要标签、单列“标签—值”弱边框编辑行
  和底部操作区；密钥字段统一显示为“API Key”；本阶段当时保留的 Provider 级启用开关已在第 16 节按默认可用语义移除，
  保存、测试、Key 清除、设为当前和危险删除的其它请求职责未改变；未新增运行状态页签；
- Agent 执行预算保持原位置、字段、恢复默认和独立保存行为；移动端标题操作区改为全宽纵向排列；
- 自动化验证通过：定向设置契约测试 `25/25`、全部前端测试 `222/222`、`scripts/run-frontend.ps1 build` 通过；构建仅有
  既有大 chunk 提示；
- 浏览器验收覆盖桌面、平板和移动断点：桌面双卡及目录/详情双栏，平板双卡且工作台上下排列，移动端双卡单列、目录仅
  保留“连接名称 / 所属 Review / 状态”；三档文档宽度均未超过可视区。浏览器只读取现有配置并切换目录选中行，未修改
  全局开关、连接配置或 Key，未调用真实 Provider、执行 Agent Review、提交、推送或部署。

## 15. Agent 执行预算推荐下拉项

### 15.1 单阶段实施边界

改动量等级：**小**。仅替换 Agent 执行预算的输入控件与局部布局，不改变预算 DTO、保存接口、执行语义或数据库结构。

实施状态：**已完成（2026-08-13）**。

目标：将八个自由整数输入调整为带单位的推荐值下拉框，降低非法组合和误输入概率，同时完整兼容服务端默认值、范围与
历史自定义值。

范围：

- 前端为八个预算字段提供稳定推荐候选值，并使用服务端 `budgetDefaults`、`budgetLimits` 动态过滤和补齐；
- 当前值不在推荐候选中时自动补为“当前自定义”选项，不覆盖或丢弃历史配置；
- 默认值在选项中标识为“默认/推荐”，三个跨字段约束对应的非法候选项禁用并展示原因，不静默联动修改其它字段；
- 保留“恢复默认运行参数”“保存执行预算”、默认/自定义标签、dirty 守卫和保存后只影响新任务的语义；
- 布局调整为桌面四列、平板两列、移动端单列。

非目标：不修改 Backend、公开 API、预算范围、默认值、跨字段校验、Agent 执行链、任务预算快照或既有历史配置。

验收：推荐值、服务端默认值和历史自定义值均可正确显示；非法组合不能被选中；八个字段继续随原 PUT 一次保存；预算
工具函数测试、设置页契约测试、全部前端测试、build 和三档浏览器验收通过。

授权边界与停止点：只允许修改本文、预算前端工具函数、设置页预算组件、样式和对应测试；不得修改 Backend、数据库、
README 或 `docs/36`，不得执行真实 Agent Review。完成验证并回填结果后停止，不自动提交、推送或部署。

### 15.2 实施结果

- 八个 Agent 执行预算字段已由自由整数输入改为带单位的推荐值 Select；桌面四列、平板两列、移动端单列，原显式保存、
  恢复默认、默认/自定义标签、dirty 和“只影响新任务”语义保持不变；
- 新增预算选项生成函数：候选值按服务端范围过滤，服务端默认值和当前值强制补齐；非推荐历史值标识为“当前自定义”，
  默认值标识为“默认/推荐”，不覆盖已保存配置；
- `maxTurns / submitByTurn`、`maxToolCalls / maxEvidenceCalls`、`maxEvidenceCalls / convergeAtCalls` 三组约束继续复用原校验，
  会使对应非法候选项禁用并通过选项标题展示原因，不静默调整其它字段；
- 自动化验证通过：定向预算与设置契约测试 `32/32`、全部前端测试 `225/225`、`scripts/run-frontend.ps1 build` 通过；
  构建仅有既有大 chunk 提示；
- 浏览器验收确认桌面八项为四列两行、平板两列四行、移动端单列八行，三档均无页面横向溢出；桌面与移动下拉层未被
  容器或视口裁切，模型回合非法候选项正确禁用并携带约束原因。验收仅展开下拉框，未变更或保存预算，未执行真实
  Agent Review、提交、推送或部署。

## 16. Standard Provider 默认可用语义

### 16.1 单阶段实施边界

改动量等级：**中**。页面上只移除一个开关，但需要同步收敛 Provider API 兼容字段、历史停用数据、Standard 执行校验、
运行总览投影和前端状态派生，确保不是只隐藏控件而保留不可见的停用状态。

实施状态：**已完成（2026-08-13）**。

目标：Standard Provider 不再提供独立启停能力；连接保存后默认处于可用候选状态，实际可执行性只由 Endpoint、模型与
API Key 配置完整性决定。平台级 Review 总开关继续决定 Standard Review 是否整体运行。

范围：

- 移除 Standard Provider 详情中的启用开关和保存请求中的 `enabled` 字段；
- Standard 目录、运行摘要和“设为当前”按钮只依据 Endpoint、模型与 API Key 完整性派生可用状态；
- Backend 创建、更新、列表、执行与运行总览不再使用持久化 `enabled` 作为 Provider 可用门禁；
- 旧 `enabled` 请求/响应字段作为兼容字段保留：请求值不再改变行为，响应统一返回 `true`，历史 `false` 数据自动按新语义
  可用，无需数据迁移；清除 API Key 后连接因配置不完整而不可用，但不再进入“停用”状态。

非目标：不移除数据库历史列，不修改平台 Review 全局开关、Agent Review 全局开关或 Agent Runtime 启停；不改变默认
Provider 解析、Key 安全边界、协议、超时、Provider 删除保护或真实模型调用协议。

验收：历史 `enabled=false` 且配置完整的 Provider 可执行并在目录中显示“可用”；清除 Key 后显示“不可用”；详情无启用
开关且保存请求不含 `enabled`；兼容 API 返回稳定；Backend 定向 contract / unit、全部前端测试、前端 build 和浏览器页面
验收通过。测试只使用虚构凭据和 mock，不请求真实 Provider，不产生模型费用。

授权边界与停止点：允许修改本文、Python Backend Provider 契约与投影、模型连接前端与对应测试；不修改数据库 schema、
README 或 `docs/36`，不调用真实 Provider，不提交、推送或部署。完成验证并回填结果后停止。

### 16.2 实施结果

- Standard Provider 详情已移除启用开关，保存详情不再发送 `enabled`；“设为 Standard 当前连接”改由 Endpoint、模型与
  已保存 API Key 的完整性控制，运行摘要显示“Provider 可用 / 配置不完整”，目录不再产生 Standard“已停用”状态；
- Provider 创建和更新统一保存 `enabled=true`；旧请求仍可携带该字段但其值不再改变行为，列表响应继续保留字段并统一
  返回 `true`。历史数据库 `enabled=false` 无需迁移，执行与修复预览已不再读取该列；清除 Key 只清空凭据并由配置完整性
  判为不可用；
- 运行总览 Provider 投影同步按默认可用输出，不会将历史 `false` 行误报为停用；平台 Review 总开关、Agent Review
  全局开关、Agent Runtime 启停和数据库 schema 均未改变；
- Backend 精确契约与单元用例 `8/8`、本次 Python 文件 Ruff、前端定向用例 `40/40`、全部前端测试 `227/227` 和
  `scripts/run-frontend.ps1 build` 均通过；build 仅有既有大 chunk 提示；
- Backend 四个相关测试文件合并运行结果为 `153 passed / 3 failed`，3 项分别是既有修复预览 404、同一用例内 Provider
  切换缓存和 Push Gate 判定问题，均不经过本次 Standard `enabled` 改动路径；随后精确用例已全部通过；
- 浏览器刷新现有 `/settings/model-connections` 后确认：Standard 详情启用文案和控件数量均为 0，API Key 与保存操作正常，
  当前运行卡显示“Provider 可用”，页面无横向溢出。验收未保存配置、清除 Key、测试连接或调用真实 Provider。

## 17. 后续专题迁移说明

“AI Review 配置入口合并”不再继续追加在本历史实施文档中，已迁移到独立专题
[`docs/58-ai-review-settings-consolidation-plan.md`](58-ai-review-settings-consolidation-plan.md)。

本文件继续作为传统后台壳层、模型连接工作台及其既有调整的历史记录；后续合并实施、阶段状态、验证结果与停止点只更新
58 号专题文档，避免执行任务时重复读取本文件的旧阶段正文。
